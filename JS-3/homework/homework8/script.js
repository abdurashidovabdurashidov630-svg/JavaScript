// Mutable
// push(), pop(), unshift(), shift(), delete,split(), join(),
// reverse(), concat(),splice();forEach(),flat(),fill(), copyWithin(),reduce(),Array.from(), Array.of()

// Immutable:
// length, at(), indexOf(), lastIndexOf(), includes(), slice(),map(),
// filter(), find(), findIndex(), findLastIndex(), sort(), every(), some(),Array.isArray()